# AI 协作过程记录

本文档用于说明本项目从需求理解、方案收敛、实现到验收的 AI 协作过程。它不是完整聊天逐字稿，而是按提交材料需要整理出的关键节点记录，重点保留：

- 我给 AI 的输入
- AI 产出的草稿 / 代码 / 检查意见
- 我做出的产品判断、范围裁剪和验收决策

项目：PM 每日跟进助手 PoC

提交内容：本地可运行 demo、SPEC、README、复盘说明、AI 协作过程记录

---

## 1. 需求理解与方案选择

**我的输入**

- 题目给出的三类信息：VOC 表、研发任务表、项目群聊
- 希望 AI 先判断适合做成什么形态，而不是直接写代码
- 明确要求保留依据、体现人工确认，避免做成一个不可解释的黑盒

**AI 输出**

- 对 dashboard、CLI、表格、LLM 自动总结等形式做了比较
- 推荐做成本地网页 dashboard
- 起草第一版 SPEC，包含 LLM、文件上传、后端存储等更完整的设想

**我的把关**

我认为第一版范围过大，不适合测试题提交，因此把目标裁剪为 v1：

- 不接 LLM
- 不做文件上传
- 不做后端和数据库
- 使用本地 mock 数据 + 纯规则
- 重点验证“系统提名，PM 拍板”的主链路

这个阶段产出最终版 [SPEC.md](./SPEC.md)。

---

## 2. 规则口径确认

**我的输入**

我要求 AI 根据示例数据梳理哪些判断可以自动做，哪些必须人工确认，尤其关注：

- 今日待回复
- 上线承诺风险
- 联调依赖风险
- 已上线未确认
- VOC 与 DEV 的疑似关联

**AI 输出**

AI 给出一版规则拆解，并把规则写进 SPEC。

**我的修正**

我重点修正了几处业务口径：

1. DEV-101 / DEV-106 不简单归为“延期风险”，而是“上线承诺风险 / 联调依赖风险”。
2. DEV-104 的“已上线未确认”应来自 DEV 备注和群聊，不应根据关联 VOC 的“已确认”状态推断。
3. VOC-006 与 DEV-105 的关系来自群聊 10:03，需要进入“待人工确认”，不能被漏掉。

这些修正确保系统不会替 PM 直接拍板高风险判断。

---

## 3. 实现顺序设计

**我的输入**

我先提出一个实现拆法：

1. mock 数据
2. 规则函数
3. 控制台验证
4. UI 四块区域
5. 人工确认按钮
6. 导出 markdown
7. README 和演示脚本

**AI 输出**

AI 建议先定好全局 state 形状，并尽早抽出两个公共能力：

- `compute()`：让 UI 和验证脚本共用同一套计算逻辑
- `resolveEvidence()`：所有结论都能根据 evidence id 反查原始 VOC / DEV / 群聊

**我的决策**

接受这个建议。这样可以避免 UI 和验证脚本各算一套，减少“界面看起来对，但验收逻辑不一致”的风险。

---

## 4. 规则层与验证脚本

**我的输入**

要求 AI 先实现主链路，而不是先做页面样式：

- 把 VOC / DEV / 群聊转成 mock 数据
- 写纯规则函数
- 用脚本验证 SPEC §8 的最小验收标准

**AI 输出**

AI 完成：

- `src/data/*` mock 数据
- `src/rules/*` 规则层
- `src/compose.js` 汇总计算
- `src/lib/evidence.js` 依据解析
- `verify.js` 自动验收脚本

**人工验收**

我用 `npm run verify` 检查规则结果，重点确认：

- 今日待办包含 VOC-005、VOC-007、VOC-008
- 风险包含 DEV-104、DEV-101、DEV-106
- 待人工确认包含 VOC-003 ↔ DEV-103、VOC-006 ↔ DEV-105
- 所有 evidence id 都能反查原文

---

## 5. UI 与人工确认闭环

**我的输入**

要求 AI 把规则结果做成单页 demo，页面分为：

- 今日摘要
- 今日待办
- 风险
- 待人工确认

每条结论必须显示依据，点击依据能看到原文。

**AI 输出**

AI 完成 React 页面和组件拆分：

- `Summary`
- `Todos`
- `Risks`
- `Pending`
- `EvidenceTag`

随后加入 `useReducer` 管理前端状态，让 PM 可以：

- 标记待办已回复
- 确认或否定 VOC ↔ DEV 疑似关联
- 标记 DEV-104 已经客户确认
- 重置状态

**我的把关**

我检查交互是否符合 HITL 思路：系统只提名，PM 点击确认后页面重新计算。尤其确认 DEV-104 后，相关“已上线未确认”风险和待确认项要一起消失。

---

## 6. 导出与提交说明

**我的输入**

要求 AI 补齐提交材料，使评审可以快速理解和复现：

- 导出当前页面结果为 markdown
- README 写清楚怎么运行
- README 给出 3 分钟演示脚本
- 补充复盘说明

**AI 输出**

AI 完成：

- `src/lib/exportMd.js`
- `ExportModal`
- [README.md](./README.md)
- [RETROSPECTIVE.md](./RETROSPECTIVE.md)
- 页面截图 `docs/screenshot.png`

**我的验收**

我按 README 的演示脚本手动跑了一遍：

1. 看摘要数字
2. 看 3 条今日待办
3. 看 4 条风险
4. 看 3 条待人工确认
5. 点击 evidence 展开原文
6. 确认 VOC-003 ↔ DEV-103
7. 标记 DEV-104 客户已确认
8. 标记一条待办已回复
9. 导出 markdown
10. 重置状态

---

## 7. AI 自检与我最终拍板

开发过程中，AI 多次做 self-review，并指出过一些需要修的点，例如：

- 群聊 evidence id 只用 `HH:MM` 不够唯一，改成 `chat@YYYY-MM-DD HH:MM`
- 依赖关系不能只抓第一个 DEV id，要支持多个匹配
- 按钮 action 形状需要统一
- evidence 展开组件需要避免重复逻辑

我没有直接接受“能跑就行”的版本，而是要求先清掉影响验收和可读性的 self-review findings，再进入最终提交。

最终保留的验证方式：

- `npm run verify`
- `npm run build`
- 手动 demo 检查

---

## 8. 本项目中 AI 的作用

AI 主要承担：

- 把题目材料转成结构化 SPEC
- 提供实现拆解建议
- 生成规则层、React 组件和导出逻辑
- 做代码 self-review
- 帮助整理 README、复盘说明和本记录

我主要承担：

- 判断产品范围
- 修正规则口径
- 决定哪些能力进入 v1，哪些推到 v2
- 检查 HITL 是否成立
- 按验收脚本和手动 demo 做最终拍板

这次协作的核心不是让 AI 一次性“自动完成产品”，而是让 AI 快速产出草稿和实现，我在关键判断点上收敛范围、校准业务逻辑，并保证最终 demo 能被评审复现。

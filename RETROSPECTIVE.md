# 复盘说明

## 这个项目为什么这样做

### 1. 先写 SPEC，不是先写代码

题目信息量不小（三张表 + 11 行群聊），直接动手很容易做出"功能堆砌但抓不住核心"的东西。先写 SPEC 把要解决什么、做什么、不做什么、哪些可以自动、哪些必须人工，全部明确下来，后续每一步都能对照 SPEC 检查"这是不是当时要做的"。

写 SPEC 的过程本身就是想清楚边界的过程：第一版 SPEC 我读完觉得做大了，砍掉 LLM 和上传缩到 v1，这就是 SPEC 阶段的价值——便宜地试错。

### 2. 先做规则层和 verify.js，不是先做 UI

规则层（VOC → todo、DEV → 风险、群聊 → 信号、依赖回溯）是这个工具的"脑子"，UI 只是壳。先把脑子做对、用 `verify.js` 把 SPEC §8 的 7 条验收变成 12 个 assert，跑通了主逻辑就立住了——后面做 UI 是"包装"，挂错壳子是低风险事。

如果反过来先做漂亮 UI，规则层有缺陷时很容易"看着对其实不对"，调试成本会高很多。

### 3. 没接 LLM，改成纯规则版

两个原因：

- **本地运行更稳**：评审/演示时不需要 API key、不需要外网，谁拿到代码 `npm install && npm run dev` 就能看到，反馈环也快。
- **看清规则边界**：纯规则版能让人很清楚地看到"哪些判断系统能自动做、哪些必须人工"。如果一开始就用 LLM，HITL 的边界容易被 LLM 的"看起来挺合理"模糊掉。

代码里 `parseChat` 的签名是 `(chat, voc, dev) → 信号`，v2 换成 LLM 实现，上层 `todos` / `risks` / `pending` 不用改。这个口子是有意留的。

### 4. 砍掉的功能

判断原则：不影响主链路就砍，影响主链路就保。

| 砍掉 | 理由 |
|---|---|
| 文件上传 | mock 写代码里就行，省一个完整上传交互 |
| 数据库 / 后端持久化 | 前端 state 就够演示 HITL 闭环，刷新丢是预期 |
| 真实 IM 接入 | 群聊数组化就够 |
| 登录 / 权限 | 单人 demo |

保留：依据 ID、HITL 按钮、导出 markdown —— 这三个都直接影响"PM 拿到这个能不能用"。

### 5. 这个 PoC 验证的核心

**"系统提名 + PM 拍板"** 这个模式能不能跑通。

具体来说：

- 系统能不能从散乱数据里提名出有用的待办、风险、关联？→ verify 12/12 验证了能
- 依据能不能让 PM 反查到原始 VOC / DEV / 群聊？→ EvidenceTag 点击展开验证了能
- PM 改了一个判断，整页能不能立即重新算？→ useReducer + compute 验证了能
- 能不能产出一份能直接发群里的 markdown？→ ExportModal 验证了能

不验证的：自动化判断的**准确率**。这是 v2 接 LLM 之后才需要严肃评估的事，v1 的纯规则版准确率受限于规则覆盖度，本来就不是重点。

### 6. 如果多一天我会补什么

- **文件上传**：把三类数据从 mock 改成上传 CSV / 粘贴文本，真实 PM 能跑自己的数据，PoC 价值跨一大步。
- **状态持久化**：localStorage 起步，刷新不丢；后续有后端就走数据库 + 多用户。
- **更智能的群聊解析**：把 `parseChat` 换成 LLM 调用（已经留好口子）。关键不是"用了 LLM"本身，而是要让 LLM 输出**结构化 + 带置信度**：低置信度自动进"待人工确认"，不是让 LLM 直接拍板。这样 HITL 的边界依然清晰。

这三个加起来基本就是 v2 的雏形了。

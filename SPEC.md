# PM 每日跟进助手 PoC — SPEC（v1 / 纯规则版）

## 1. 要解决什么问题

PM 每天要从 VOC 表、研发任务表、项目群聊里把信息凑起来，才知道：
- 今天哪些客户在等我回复
- 哪些功能可能延期 / 已经上线但客户没确认
- 哪些 VOC 和 DEV 是同一件事但没挂上关联

现在这件事完全手工，三类数据各看一遍，容易漏。这个 PoC 想做的是：
**把三类数据吃进来，自动出一份"今天该做什么"的草稿，PM 在上面确认/修改即可。**

v1 的目标只是**把主链路跑通**：用写死的 mock 数据 + 纯规则，证明这个流程对 PM 有用。LLM、文件上传、数据库都不做。

## 2. 目标用户和任务

用户：产品经理（示例数据里的产品 A/B/C/D）。
典型场景：每天上午打开工具 → 看今天的待回复 / 风险 / 待确认关联 → 该回的回、该改的改 → 导出一份当日跟进清单。

## 3. PoC 做什么 / 不做什么

**做：**
- 三类数据用 **mock 常量**直接写在代码里（VOC、DEV、群聊各一个数组）
- 用**纯规则**推导：待回复、上线承诺风险、联调依赖风险、已上线未确认、VOC-DEV 疑似关联
- 群聊解析用**关键词/正则**匹配 VOC/DEV id 和几个固定信号词（"已上线"、"追问"、"应该和…是同一个"、"待回复"等）
- 给每条结论附**依据**（VOC-005 / DEV-101 / 群聊 09:12）
- PM 在界面上**确认**疑似关联、**勾选**客户已确认、**标记**已回复（状态只存在前端 state 里，刷新会丢，这是预期的）
- 导出今日跟进清单（markdown，复制或下载都行）

**不做（明确推后到 v2）：**
- 不接 LLM —— 群聊解析、回复要点、摘要都先用规则/模板
- 不做文件上传 —— 数据写死在代码里
- 不做数据库/后端持久化 —— 前端 state 模拟即可，刷新丢失没关系
- 不接真实 IM、不自动给客户发回复
- 不做权限、协作、历史趋势、图表

## 4. 用户使用流程

1. 启动应用，浏览器打开
2. 页面默认就显示基于 mock 数据 + 规则算出来的今日结果，**无需任何上传或配置**
3. 页面分四块：
   - 今日摘要（一段话）
   - 今日待办（按客户分组）
   - 风险
   - 待人工确认
4. PM 在"待人工确认"里点 ✅/❌，在"待办"里勾"已回复"——界面立刻反应（待办减少、风险消失等）
5. 点"导出"，得到一份当日清单（markdown）

刷新页面就回到初始状态，v1 不做持久化。

## 5. 三类数据怎么处理

| 数据 | 角色 | v1 处理方式 |
|---|---|---|
| VOC 表 | 客户诉求源 | 写成数组常量，字段照原表；按 `状态` + `优先级` 过滤 |
| DEV 表 | 研发进展源 | 写成数组常量；按 `研发状态` + `预计完成时间` + `备注` 推导风险 |
| 群聊 | 最新动态、隐含关联和确认信号 | 写成结构化数组（每条 `{时间, 发言人, 内容}`），用**正则**抽 `VOC-\d+` / `DEV-\d+`，用**关键词**判断信号类型 |

群聊关键词规则（v1 用这些就够）：
- 含 "已上线" + DEV id → 标记该 DEV 为"群里提到已上线"
- 含 "没有客户确认" / "未确认" + DEV id → 标记该 DEV 为"客户未确认上线"
- 含 "追问" / "再问" / "什么时候" / "明确上线时间" + 客户名或 VOC id → 加权该 VOC 为今日待回复，并把对应 DEV 标记为"上线承诺风险"
- 含 "应该和" / "是同一个" / "感觉和…有关系" + VOC id + DEV id → 生成"疑似关联"建议
- 含 "依赖" + DEV id → 标记 DEV 间依赖；被依赖方未完成 → 上游 DEV 进入"联调依赖风险"
- 含 "本周" / "今天" / "下班前" → 标记紧急

规则匹配不到的群聊行，原文照样保留，作为"其他动态"显示，不要丢掉。

## 6. 自动 vs 人工

**自动（规则可以判断）：**
- VOC `状态=待回复` 且 `优先级∈{P0,P1}` → 今日待办
- DEV `预计完成时间 < today` 且 `研发状态 ∉ {已上线}` → 延期风险
- DEV 未到预计完成时间，但群聊里客户在追问明确上线时间（命中"追问/明确上线时间"关键词） → **上线承诺风险**
- DEV `备注` 含"依赖 DEV-xxx" 或群聊出现"依赖 DEV-xxx"，且被依赖方未完成 → **联调依赖风险**
- DEV `备注` 含"待客户确认" 或群聊命中"没有客户确认/未确认"关键词 → **已上线未确认**（**不**通过关联 VOC 的状态判断，因为 VOC 的"已确认"只表示需求确认，不代表客户确认上线）
- 群聊里同一句出现 VOC id 和 DEV id 且 DEV 当前未挂该 VOC → 疑似关联候选
- 群聊关键词命中（见上节）→ 在对应 VOC/DEV 上加标签

**必须人工（系统只提名，PM 拍板）：**
- 疑似 VOC ↔ DEV 关联是否成立（点 ✅ 才生效）
- "已上线"功能客户是否真的确认过（PM 勾选）
- 今日待办是否真的要回（PM 可以标记"已回复"或"推迟"）

原则：**系统提名，PM 拍板。** 任何错了代价高的判断都走人工。

## 7. 怎么呈现

四块从上到下：

**① 今日摘要**（模板拼出来的一段话，不用 LLM）
> 今天有 N 条 P0/P1 客户待回复（按客户列出），M 个研发任务有上线承诺/联调依赖风险（列 DEV id），K 个功能已上线待客户确认（列 DEV id），J 条 VOC 疑似缺关联。

数字和 id 都从规则结果里 fill 进去。

**② 今日待办**（按客户分组的卡片）
每张卡片：
- 客户 + 优先级（P0 标红）
- VOC 内容（截断显示，可展开）
- **依据**：VOC-005 + 群聊 09:12（点击展开看原文）
- **建议回复要点**：v1 用模板生成，例如：
  - 跟进类 + 关联 DEV 在开发中："告知客户 [DEV 标题] 预计 [日期] 提测，[日期] 客户侧可见"
  - 跟进类 + 无关联 DEV："说明目前未排期，给一个明确的回复时间"
  - 含"如果…希望先…"："回复是否能提供临时方案"
- 操作：[已回复] [推迟]

**③ 风险**
每条：风险类型（上线承诺 / 联调依赖 / 延期 / 上线未确认 / 未关联）+ 涉及 id + 依据 + 建议动作。

**④ 待人工确认**
- 疑似关联：`VOC-003 ↔ DEV-103`（依据：群聊 2026-05-16 09:24）→ [确认] [否]
- 疑似关联：`VOC-006 ↔ DEV-105`（依据：群聊 2026-05-16 10:03）→ [确认] [否]
- 待客户确认：`DEV-104 已上线`（依据：DEV-104 备注"待客户确认" + 群聊 09:18）→ [已确认] [未确认]

**所有结论必须显示依据 id**，点击能看到原文（VOC 行 / DEV 行 / 群聊原句）。这是 v1 的硬要求。

## 8. 最小验收标准

用题目里给的示例数据，假设 today = 2026-05-16，跑一次要能：

1. 今日摘要里至少包含：沃尔玛 VOC-005 待回复（P0）、奔驰 VOC-007 待回复（P1）、地铁 VOC-008 待回复（P1）
2. 风险里至少包含：
   - DEV-104 已上线未确认（依据：DEV-104 备注"待客户确认" + 群聊 09:18）
   - DEV-101 上线承诺风险（依据：VOC-005 + 群聊 09:12 沃尔玛追问明确上线时间）
   - DEV-106 联调依赖风险（依据：DEV-106 备注"依赖DEV-101" + 群聊 09:15）
3. 待人工确认里至少包含：
   - VOC-003 ↔ DEV-103（依据：群聊 09:24）
   - VOC-006 ↔ DEV-105（依据：群聊 10:03）
4. 每一条都能展开看到对应原文
5. PM 点"确认 VOC-003 ↔ DEV-103"后，VOC-003 不再出现在"未关联 VOC"风险里
6. PM 点"DEV-104 客户已确认"后，该条从"待确认"消失
7. 能导出一份 markdown 清单，内容和界面一致

跑通这 7 条，v1 就算成立。

## 9. 建议的实现顺序

1. **mock 数据**：把题目里 VOC / DEV / 群聊原样转成代码里的常量数组
2. **规则层**：写 4~5 个纯函数（`getTodos`、`getRisks`、`getPendingConfirmations`、`parseChat`、`getSummary`），输入 mock 数据，输出结构化结果，每条带 `evidence: ['VOC-005', 'chat@09:12']`
3. **控制台先跑通**：不要前端，先 `console.log` 看结果是否符合验收标准 1~3
4. **最简前端**：一页 HTML，四块区域渲染上一步的结果，依据可点开看原文
5. **人工确认交互**：加按钮，点击修改前端 state，重新算依赖项（待办/风险）
6. **建议回复要点**：按第 7 节的模板规则填充
7. **导出**：把当前 state 拼成 markdown

每一步都独立可验证。第 3 步跑通就已经证明核心逻辑成立，后面只是把它包成 UI。

## 10. v1 之后（不在本次范围）

留个口子，方便 v2 演进，但 v1 不做：
- 群聊解析换成 LLM（替换 `parseChat` 实现，其他不动）
- 回复要点换成 LLM（替换模板函数）
- 加文件上传 / 后端 + 数据库 / 多用户
- 历史归档、趋势分析

v1 的规则层和接口形态要写成**可替换**的，比如 `parseChat(rawText) → 结构化结果`，将来换 LLM 实现也不影响上层。

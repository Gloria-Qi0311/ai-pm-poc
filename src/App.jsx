export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: 'system-ui, sans-serif', color: '#333' }}>
      <h1>PM 每日跟进助手 PoC</h1>
      <p>v1 主链路（规则层 + verify.js）已实现。UI 见后续 issue #7–#11。</p>
      <p>当前可运行 <code>npm run verify</code> 验证规则层。</p>
    </div>
  )
}
